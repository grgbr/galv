Implement
=========

* galv_sess_close(), galv_sess_halt() galv_sess_ignore() ?
* specialize rpc client vs service xfer callback
* use a per coupler/binder/acceptor/adopter/connection ratelim logger variable
* make a galv_coupler_reconnect()/galv_clnt_reconnect() API

Testing
=======

* unix gate


Cleanup
=======

remove galv_conn_dispatch() and its callees
cleanup galv_conn_ops (and assertions intern / api)
remove all references to on_may_xfer, on_send_shut, on_recv_shut, on_error
make a per object type ratelim logger (conn, accept, coupler), should not be
necessary for binder / adopter.
implement a client reconnection strategy object ??!
rename galv_binder_on_connected() to galv_binder_load_peer()
since poller is now referenced by connection, remove all poller arguments from
API
add a connection closed state since clients / binder needs a way to destroy
connection without closing them (and modify galv_coupler_on_conn_term() to
effectively close the underlying socket)
